datasources:
  default:
    url: "jdbc:sqlite:file::memory:?foreign_keys=true"
    driverClassName: org.sqlite.JDBC
  event-publisher:
    url: "jdbc:sqlite:file::memory:?foreign_keys=true"
    driverClassName: org.sqlite.JDBC

